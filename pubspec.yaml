name: anime_app
description: تطبيق الأنمي العربي

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  cached_network_image: ^3.3.1
  video_player: ^2.8.3
  chewie: ^1.7.4
  shared_preferences: ^2.2.2
  flutter_staggered_grid_view: ^0.7.0

flutter:
  uses-material-design: true
